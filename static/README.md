# 🚀 BlastPro – Ultimate Telegram Automation SaaS

**BlastPro** adalah platform **Software as a Service (SaaS)** berbasis web yang memungkinkan **UMKM dan pebisnis** mengelola pemasaran Telegram secara **otomatis, aman, dan scalable**.

Platform ini menggunakan **arsitektur Multi-Tenant**, sehingga **satu server** dapat menangani **ribuan akun Telegram** tanpa perlu pengguna menyewa VPS sendiri.

---

## ✨ Fitur Unggulan

### 🤖 1. Multi-Tenant Userbot Core
- Web-based login Telegram (Nomor HP + OTP)
- Stateless session (aman walau server restart)
- Dukungan **2FA / Cloud Password**
- Satu server → banyak akun Telegram

---

### 🎯 2. Advanced Targeting & Scanner
- Auto scan semua grup yang diikuti akun
- **Forum Topic Support** (kirim ke topik spesifik)
- Bulk save ratusan grup target
- Database target terisolasi per user

---

### 📢 3. Smart Broadcast System
- Kirim **Text & Image**
- Personalized message (`Halo {name}`)
- Anti-Flood Mechanism (handle `FloodWaitError`)
- Background processing (UI tetap responsif)

---

### 👥 4. CRM Otomatis
- Chat Mining (scan PM Telegram)
- Simpan Username, Nama, ID user
- Database CRM per akun
- Siap untuk follow-up massal

---

### 🛡️ 5. Super Admin (God Mode)
- Panel kontrol penuh pemilik platform
- Monitor semua user & bot aktif
- **Kill Switch** (Suspend/Ban instan)
- Statistik realtime global

---

### ⚡ 6. Stabilitas Server (Anti Tidur)
- Self-ping worker setiap 14 menit
- Mencegah Free Tier (Render) masuk sleep mode
- Bot tetap aktif **24/7**

---

## 🏗️ Arsitektur & Teknologi

### Backend Stack
- **Python Flask** – Web framework
- **Telethon** – Telegram MTProto client
- **Supabase (PostgreSQL)** – Database (via REST API)
- **Asyncio Bridge** – Jalankan async Telethon di Flask
- **Gunicorn** – Production WSGI server
- **Httpx** – Self-ping & HTTP client

---

## 🗄️ Database Schema (PostgreSQL)

Relasi **One-to-Many**  
1 User → 1 Akun Telegram → Banyak Target, Jadwal & Log

### Tabel Utama
- `users` – Akun web user & admin
- `telegram_accounts` – Session Telethon
- `blast_targets` – Grup & topik target
- `blast_schedules` – Jadwal blast
- `blast_logs` – Log sukses/gagal
- `tele_users` – CRM (kontak Telegram)

---

## 📂 Struktur Folder Proyek

├── app.py # Otak utama (Flask + Bot Logic)
├── requirements.txt # Dependency Python
├── Procfile # Konfigurasi Render
│
├── static/
│ ├── css/
│ │ ├── admin.css
│ │ ├── auth.css
│ │ ├── dashboard.css
│ │ └── landing.css
│ └── uploads/ # Image broadcast sementara
│
└── templates/
├── landing/
├── auth/
├── dashboard/
└── admin/


---

## 🚀 Panduan Instalasi & Deploy

### 1️⃣ Persiapan Database (Supabase)

Buat project Supabase → SQL Editor → Jalankan script berikut:

```sql
-- USER WEB
create table public.users (
  id bigint generated by default as identity primary key,
  email text unique not null,
  password text not null,
  is_admin boolean default false,
  is_banned boolean default false,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- AKUN TELEGRAM
create table public.telegram_accounts (
  id bigint generated by default as identity primary key,
  user_id bigint references public.users(id) on delete cascade unique,
  phone_number text,
  session_string text,
  is_active boolean default true,
  targets text default '[]',
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- TARGET GRUP
create table public.blast_targets (
  id bigint generated by default as identity primary key,
  user_id bigint references public.users(id) on delete cascade,
  group_id bigint,
  group_name text,
  topic_ids text,
  is_active boolean default true,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- JADWAL
create table public.blast_schedules (
  id bigint generated by default as identity primary key,
  user_id bigint references public.users(id) on delete cascade,
  run_hour int,
  run_minute int,
  is_active boolean default true,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- LOG
create table public.blast_logs (
  id bigint generated by default as identity primary key,
  user_id bigint references public.users(id) on delete cascade,
  group_name text,
  group_id bigint,
  topic_id int,
  status text,
  error_message text,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- CRM
create table public.tele_users (
  id bigint generated by default as identity primary key,
  owner_id bigint references public.users(id) on delete cascade,
  user_id bigint,
  username text,
  first_name text,
  last_interaction timestamp with time zone,
  created_at timestamp with time zone default timezone('utc'::text, now()),
  unique(owner_id, user_id)
); 
`` –

2️⃣ Deploy ke Render.com

Upload kode ke GitHub Repository

Buat Web Service di Render

Setting:

Environment: Python 3

Build Command: pip install -r requirements.txt

Start Command: gunicorn app:app

Environment Variables
Variable	Deskripsi
SUPABASE_URL	URL Supabase
SUPABASE_KEY	API Key
API_ID	Telegram API ID
API_HASH	Telegram API Hash
SECRET_KEY	Flask Secret
SUPER_ADMIN	Email admin
PASS_ADMIN	Password admin
PYTHON_VERSION	3.9.0
3️⃣ Akses & Penggunaan

Admin Panel: /super-admin

Register User: /register

Login Telegram via dashboard

Scan grup → atur jadwal → blast

🔒 Keamanan & Disclaimer

Session Telegram disimpan di database Anda sendiri

Jangan bocorkan SUPABASE_KEY

Blast berlebihan berisiko limit/banned Telegram

Gunakan interval & konten yang wajar

📜 Lisensi

Proyek ini dibuat untuk edukasi & produktivitas bisnis Baba Parfume.

❤️ Credit

Dibuat dengan ❤️ dan Kopi ☕
BlastPro – Telegram Automation Made Easy
